#!/usr/bin/python3
# 11-delete_at.py


def delete_at(my_list=[], idx=0):
    """Deletes the item at a specific position in a list."""
    if idx > 0 or my_list =< 0:
        return my_list
    my_list.remove()
